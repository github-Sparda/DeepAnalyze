# DeepAnalyze 核心依赖 (API调用模式优化版)
# 专为通过外部API服务进行数据分析而设计

# 数据处理基础
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
python-dotenv

# 数据可视化
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.15.0

# Web服务框架
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6
websockets>=11.0

# API客户端
openai>=1.3.0
requests>=2.31.0

# 智能体编排
langgraph>=0.0.39
langchain-core>=0.1.0

# 报告生成
markdown>=3.4.0
python-docx>=0.8.11
weasyprint>=60.0  # PDF生成，可选

# 注意：移除了以下不必要的依赖：
# - torch: 仅用于本地模型推理，我们现在使用API调用
# - transformers: 仅用于本地模型加载，我们现在使用API调用
# - 任何与本地模型训练/部署相关的包

# 如果需要本地模型支持，可以额外安装：
# torch>=2.1.0
# transformers>=4.35.0
# vllm>=0.2.0  # 本地推理加速